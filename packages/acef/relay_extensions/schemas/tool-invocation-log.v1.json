{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://relay.epochly.com/schemas/x-relay/tool-invocation-log.v1.json",
  "title": "x-relay/tool-invocation-log",
  "description": "Relay ToolCall extension for ACEF bundles. Spec table line 865: include tool, args hash, result hash, idempotency key, side-effect policy. Generated under bundle.namespaces['x-relay']['tool-invocation-log'].",
  "type": "object",
  "additionalProperties": false,
  "required": [
    "schema_version",
    "tool_name",
    "args_digest",
    "result_digest",
    "idempotency_key",
    "side_effect_class",
    "invoked_at"
  ],
  "properties": {
    "schema_version": {
      "type": "string",
      "const": "x-relay.tool-invocation-log.v1"
    },
    "tool_name": {
      "type": "string",
      "minLength": 1
    },
    "args_digest": {
      "type": "string",
      "pattern": "^[0-9a-f]{64}$",
      "description": "sha256 of canonicalised tool arguments."
    },
    "result_digest": {
      "type": "string",
      "pattern": "^[0-9a-f]{64}$",
      "description": "sha256 of canonicalised tool result."
    },
    "idempotency_key": {
      "type": "string",
      "minLength": 1,
      "description": "Caller-supplied key per spec X side-effect idempotency contract."
    },
    "side_effect_class": {
      "type": "string",
      "enum": ["none", "reversible", "mutating", "external_irreversible"],
      "description": "Per spec X / E.3 side-effect classification."
    },
    "invoked_at": {
      "type": "string",
      "format": "date-time"
    },
    "pre_action_marker_digest": {
      "type": ["string", "null"],
      "pattern": "^[0-9a-f]{64}$",
      "description": "sha256 of the pre-action marker (CLAUDE.md keystone #6)."
    },
    "post_success_proof_digest": {
      "type": ["string", "null"],
      "pattern": "^[0-9a-f]{64}$",
      "description": "sha256 of the post-success proof (CLAUDE.md keystone #6)."
    }
  }
}
